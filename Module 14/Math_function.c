#include<stdio.h>
#include<math.h>
#include<stdlib.h>
int main()
{
	double x,y;
	scanf("%lf ",&x);
	// int ans=ceil(x);
	// int ans=floor(x);
	// int ans=round(x);
	// int ans=sqrt(x);
	int ans=abs(x);
	printf("%d\n",ans);
return 0;

}